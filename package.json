{
  "name": "ietc-electron-todolist",
  "version": "1.0.0",
  "description": "",
  "main": "dist/main/index.js",
  "scripts": {
    "build:preload": "tsc -b src/preload && tsc-alias -p src/preload/tsconfig.json",
    "build:main": "tsc -b src/main && tsc-alias -p src/main/tsconfig.json",
    "build:renderer": "cd src/renderer && npx ng build --base-href ./",
    "start": "npm run build:preload && npm run build:main && npm run build:renderer && electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "electron": "^41.1.1",
    "tsc-alias": "^1.8.16",
    "typescript": "^6.0.2"
  }
}
